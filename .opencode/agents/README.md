# OpenCode agents (Phase 0.5 wires these per AGENTS §85)

Planned: researcher (no code changes) · architect · frontend · rust · media · ai · ux · qa · performance · security · reviewer.
Each agent gets scoped permissions + model selection. This file prevents an empty directory from being pruned; real agent configs land with the architecture proposal.
