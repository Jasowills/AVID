---
description: Produce a concise implementation plan for a task
---
# /plan — $ARGUMENTS

1. Read `AGENTS.md`, `PLAN.md`, `PROGRESS.md`, relevant ADRs, current code + tests.
2. Output: goal, scope/non-goals, file-by-file changes, tests to add, risks, verification steps.
3. No code changes. Save significant plans to `docs/decisions/` as proposed ADRs.
