# Cross-cutting tests (Phase 1+)

E2E workflow lives here: create → import → transcribe → cut → caption → visual → export → verify output (AGENTS §94). Deterministic fixtures only. Scaffold holds the directory; harnesses land with their phases.
