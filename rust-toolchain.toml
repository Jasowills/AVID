[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
# Pinned 2026-09-20 (stable). MSRV floor for Tauri shell plugin work is 1.77.2;
# we target 1.85+ language features for the 2.12 era. See docs/research/tauri.md §1.
# CI installs exactly this toolchain. Never assume the dev machine's default.
