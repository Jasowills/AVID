/**
 * @avid/templates — scaffold only (Phase 0).
 * Template loading/validation (`template.json` + assets/scenes/rules)
 * lands in Phase 9. Applying a template produces undoable commands.
 */

export const SCAFFOLD = "@avid/templates scaffold" as const;
