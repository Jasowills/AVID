/**
 * @avid/design-system — scaffold only (Phase 0).
 * Tokens (colors, typography, spacing, radius, borders, shadows) land in
 * Phase 1. All UI imports tokens from here — no ad-hoc Tailwind values
 * for themed properties (see CONTRIBUTING.md).
 */

export const SCAFFOLD = "@avid/design-system scaffold" as const;
