/**
 * @avid/ui — scaffold only (Phase 0).
 * Shared React components land in Phase 1. May import
 * `@avid/design-system` only — never direct provider/media logic.
 */

export const SCAFFOLD = "@avid/ui scaffold" as const;
