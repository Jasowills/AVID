//! `avid` CLI entry point — post-MVP stub (AGENTS §112).
//! Prints help until the core engine stabilises. No logic yet.

fn main() {
    println!(
        "AVID CLI scaffold — commands land after the core engine is stable. See PLAN.md Phase 10+."
    );
}
